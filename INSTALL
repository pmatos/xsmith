## -*- mode: Text -*-

To install, first install Racket.

`cd` into the `xsmith` directory and then run `raco pkg install`.

`cd` into the `xsmith-examples` directory and then run `raco pkg install`.

## End of file.
